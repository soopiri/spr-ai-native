# 프로젝트 개발 지침

## 1. 프로젝트 개요

`spr-ai-native` — AI 코딩 에이전트(Claude Code / Codex CLI / Cursor)용 개발 규칙 문서와 서브에이전트 프리셋을 생성하는 npm CLI. npm에 공개 배포 중입니다.

핵심 설계: **역할 본문은 도구 중립으로 한 번만 작성하고(`preset/common/`), 도구별 차이는 어댑터(`src/targets/*.js`)가 흡수한다.** 생성물은 `pm` → `planner` → `engineer` → `qa` 4개 역할의 개발 워크플로우입니다.

## 2. 기술 스택

| 항목 | 값 |
|---|---|
| 언어 / 런타임 | 순수 JavaScript (ESM), Node 18+ |
| 의존성 | **없음.** 런타임·개발 의존성 모두 0개 |
| 테스트 | `node --test` (Node 내장) |
| 배포 | npm (`spr-ai-native`, 스코프 없음) |

TypeScript, 빌드 단계, CLI 라이브러리(commander 등)를 쓰지 않습니다. 의도된 선택입니다.

## 3. 디렉터리 구조

```
bin/cli.js              인자 파싱 · 사용법 · 결과 출력 (사용자에게 보이는 문자열은 여기)
src/index.js            타깃 디스패치 + 오타 제안(Levenshtein)
src/lib/preset.js       frontmatter 파싱 · {{}} 치환 · 프로젝트 문서 조립
src/lib/write.js        skip / force / dry-run 파일 생성기
src/targets/*.js        도구별 어댑터 — 파일 경로, frontmatter 필드, TOML 변환 소유
preset/common/
  base-rules.md         공통 행동 지침 (도구 무관)
  project-doc.md        프로젝트 지침 템플릿 §1~§8
  roles/*.md            서브에이전트 정의 (도구 중립 본문)
  commands/*.md         진입점 정의 (도구 중립 본문)
test/cli.test.js        전체 테스트
```

## 4. 검증 명령

| 목적 | 명령 | 필수 |
|---|---|---|
| 테스트 | `node --test` | 예 |
| 생성물 육안 확인 | `node bin/cli.js <target> --dry-run` | 아니오 |
| 패키지 내용 확인 | `npm pack --dry-run` | 아니오 (`files` 변경 시 필수) |

린트·타입 체크·포맷터·빌드는 없습니다. **없는 도구를 추측해 실행하지 마세요.**

## 5. 산출물 규약

`/pm` 워크플로우로 작업할 경우 산출물은 `works/<task_id>/`에 모입니다. 파일별 작성자는 `preset/common/project-doc.md` §5 표와 동일합니다. `works/`는 gitignore하지 않았으므로 커밋 여부는 사람이 판단합니다.

## 6. 아키텍처 규칙

- **역할 본문은 `preset/common/`에만 있고 도구 중립이어야 합니다.** 특정 스택 용어(pytest, ruff, mypy, Alembic, npm 스크립트명 등)를 넣지 마세요. 스택 의존 정보는 생성되는 프로젝트 지침 문서 §4·§6에 사용자가 채웁니다.
- **도구별 메타데이터(`tools`, `model`, `readonly`, TOML 변환, 파일 경로)는 `src/targets/*.js`가 소유합니다.** 프리셋 본문에 도구 이름을 하드코딩하지 마세요.
- 도구별로 달라지는 문장은 플레이스홀더로 처리합니다: `{{PROJECT_DOC}}`, `{{ARGS}}`, `{{DELEGATE_HINT}}`, `{{DELEGATE_HOWTO}}`. `fill()`은 정의되지 않은 플레이스홀더를 만나면 **예외를 던집니다** — 어댑터에 값을 추가하지 않으면 조용히 통과하지 않습니다.
- **역할 본문에 `'''`를 쓰지 마세요.** Codex 어댑터가 TOML 리터럴 문자열로 감싸므로 깨집니다. `codex.js`가 런타임에 검사하고 테스트도 확인합니다.
- **§4는 "무엇을 검증할지"(사람)와 "그 명령이 무엇인지"(에이전트)를 분리합니다.** `실행`(필수/선택/안 함)은 사람이 정하고, 명령은 에이전트가 매니페스트·CI·도구 설정에서 **근거를 찾아** 채웁니다.
- **근거 없는 실행을 금지하는 것이 설계 의도입니다.** 설정 파일에서 찾은 명령은 사실 확인(발견)이지만, "아마 이 명령일 것"은 추측입니다. 근거를 못 찾으면 반드시 **N/A로 보고**하게 하세요. 검증이 조용히 생략되는 것보다 드러나는 편이 안전합니다. 이 규칙을 완화하지 마세요.
- 발견을 허용하면서 생긴 안전장치 3개(감시 모드 명령 금지, 코드 자동 수정 옵션 금지, 환경 미비는 FAIL이 아니라 N/A)를 제거하지 마세요. 각각 무한 대기, qa의 코드 수정 금지 위반, 고칠 수 없는 실패의 fix 루프를 막습니다.

## 7. 금지 사항

- **git 커밋 / 푸시 금지.** 커밋은 사용자가 직접 합니다.
- `npm publish` 금지. 2FA OTP가 필요하고 되돌릴 수 없습니다.
- 테스트를 skip / 주석 처리해 우회하지 마세요.
- `package.json`의 `files`는 화이트리스트입니다. **새 디렉터리를 추가하면 `files`도 갱신**하고 `npm pack --dry-run`으로 확인하세요. 빠뜨리면 패키지에서 조용히 사라집니다.

### 의도적으로 하지 않기로 결정한 것

아래는 논의 후 **일부러 제외한** 것입니다. "친절하게" 다시 추가하지 마세요. 필요해졌다면 먼저 사용자에게 근거를 물으세요.

| 하지 않은 것 | 이유 |
|---|---|
| `all` 타깃 | 한 번에 한 도구만 생성하는 것이 목적. 여러 도구는 각각 실행 |
| `--lang` 옵션 | 한국어 고정. 템플릿 2배 유지보수를 감수할 이유가 아직 없음 |
| `--model` 옵션 | 생성 후 파일 한 줄 고치면 되는 일. CLI 표면적만 늘어남 |
| 기존 파일 자동 덮어쓰기 | skip이 기본, `--force`는 옵트인. 사용자의 `CLAUDE.md`를 날리는 것이 최악 |
| 마커 블록(`<!-- spr:begin -->`) 부분 갱신 | 초기 버전에 과함 |
| `.gitignore` 자동 수정 | `works/` 커밋 여부는 팀이 결정할 일 |
| `.codex/config.toml` 자동 수정 | 기존 설정 파괴 위험이 이득보다 큼. `agents.enabled` 기본값이 true |
| Cursor `--global` 파일 생성 | Cursor 전역 규칙은 파일이 아니라 Settings의 User Rules. 안내만 출력 |
| PM을 서브에이전트로 정의 | 서브에이전트의 중첩 위임은 도구별로 보장되지 않음. PM은 메인 세션 역할(커맨드/스킬 진입)로 두어 위임 depth를 1로 유지 |
| `architect` 역할 | 범용 프리셋에는 과함. 참고한 원본에는 있었으나 제외 |

## 8. 컨벤션

- **문서·주석·커밋 메시지 본문은 한국어.** 코드 식별자와 파일 경로는 영어.
- 주석은 최소로. 코드가 설명하지 못하는 "왜"만 적습니다.
- 사용자에게 출력되는 문자열은 `bin/cli.js`와 각 어댑터의 `notes`에 모읍니다.
- 버전: 생성물(프리셋 내용·파일 구성)이 바뀌면 **minor**. CLI 내부 수정만이면 patch.

## 9. 공통 행동 지침

이 저장소의 `preset/common/base-rules.md`를 따릅니다. (자기 프리셋을 자기에게 적용)
